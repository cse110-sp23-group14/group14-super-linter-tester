// This is a config file for eslint,
// a plugin deployed in .codeclimate.yml

module.exports = {
    root: true,
    env: {
      browser: true,
      node: true,
    },
    rules: {
      // Add custom rules here
    },
  };
  